_target_: local_encoder.default.LocalEncoder
vocab_size: ${vocab_size}
pad_token_id: ${pad_token_id}
embed_dim: ${embed_dim}
num_heads: ${num_heads}
ffn_dim: ${ffn_dim}
num_layers: 4
local_window_size: 16
merge_group_dim: 64
max_seq_len: ${max_seq_len}
compression_ratio_mean: 0.5
compression_ratio_min: 0.4
compression_ratio_max: 0.6
