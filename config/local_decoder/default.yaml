_target_: local_decoder.default.LocalDecoder
vocab_size: ${vocab_size}
embed_dim: ${embed_dim}
num_heads: ${num_heads}
head_dim: ${head_dim}
ffn_dim: ${ffn_dim}
num_layers: 2
local_window_size: 16
max_seq_len: ${max_seq_len}
dropout: ${dropout}
